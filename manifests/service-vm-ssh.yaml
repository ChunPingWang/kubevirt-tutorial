apiVersion: v1
kind: Service
metadata:
  name: testvm-ssh
spec:
  type: NodePort
  selector:
    kubevirt.io/vm: testvm-cirros
  ports:
    - port: 22
      targetPort: 22
      nodePort: 30000
      protocol: TCP
